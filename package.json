{
  "name": "investradar-serverless-functions",
  "version": "1.0.0",
  "description": "Server-side functions for the InvestRadar application. This file specifies only the dependencies needed for the API routes.",
  "scripts": {
    "build": "mkdir -p dist && cp index.html index.tsx App.tsx types.ts metadata.json dist/ && cp -r components services dist/"
  },
  "dependencies": {
    "@google/genai": "^1.9.0",
    "@netlify/functions": "^4.1.11"
  }
}
