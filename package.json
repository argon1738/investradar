{
  "name": "investradar-serverless-functions",
  "version": "1.0.0",
  "description": "Server-side functions for the InvestRadar application.",
  "scripts": {
    "build": "mkdir -p dist && esbuild index.tsx --bundle --outfile=dist/bundle.js --minify && cp index.html dist/"
  },
  "dependencies": {
    "@google/genai": "^1.9.0",
    "@netlify/functions": "^4.1.11"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
